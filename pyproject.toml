[project]
name = "creator-insights"
version = "0.1.0"
description = "Daily content insight collection for pet-cat creators."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
creator-insights = "creator_insights.cli:main"

[tool.ruff]
line-length = 100
target-version = "py310"

